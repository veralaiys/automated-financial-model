# automated-financial-model
Python script that pulls Apple's financials, builds a 3-statement model + DCF valuation, and auto-generates a formatted Excel workbook using yfinance, pandas, and openpyxl.
